ジャンル編集
ジャンル
<%= form_for @genre, url:admin_genre_path(@genre) do |f| %>
<%= f.text_field :name %>
<%= f.radio_button :is_active, :true, :checked => true %> 有効
  <%= f.radio_button :is_active, :false  %> 無効
<%= f.submit "変更を保存する", class:"btn btn-primary btn-xs" %>
<% end %>